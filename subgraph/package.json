{
  "name": "autopoietic",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service jernejc/apoietic",
    "create-local": "graph create --node http://localhost:8020/ autopoietic.art",
    "remove-local": "graph remove --node http://localhost:8020/ autopoietic.art",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 autopoietic.art"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.33.0",
    "@graphprotocol/graph-ts": "^0.27.0"
  }
}