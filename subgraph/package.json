{
  "name": "pixelworld",
  "version": "0.1.0",
  "scripts": {
    "create": "graph create jernejc/pixelworld --node https://api.thegraph.com/deploy/",
    "create-local": "graph create jernejc/pixelworld --node http://127.0.0.1:8020",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pixelworld"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.22.0",
    "@graphprotocol/graph-ts": "^0.22.0"
  }
}
